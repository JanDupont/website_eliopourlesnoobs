import React from 'react'

const SpellTooltip = (props) => {
    return (
        <div className="TooltipContainer">
            <img src={props.Bild} className="TooltipImage"/>     
        </div>
    )
}

export default SpellTooltip